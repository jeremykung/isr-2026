function getFormData() {
    const inputName = document.querySelector("#name").value
    console.log("Input name is:", inputName)

    // Modify this function so that it gets ALL the form data, and logs it
}