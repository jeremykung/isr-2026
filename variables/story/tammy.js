const mainCharacter = "anars"
const doing = " is buying "
const what = " ice cream"
const where = " in the van"
const then = " then Anars gets stolen by the"
const who = " truck man "
const after = " Anars is never seen again."
console.log(mainCharacter + doing + what+ where + then + what + who + after)